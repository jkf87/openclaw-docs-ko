import{_ as a,o as e,c as t,ag as i}from"./chunks/framework.CTpQozEL.js";const u=JSON.parse('{"title":"status","description":"`openclaw status`에 대한 CLI 참조 (진단, 프로브, 사용량 스냅샷)","frontmatter":{"title":"status","description":"`openclaw status`에 대한 CLI 참조 (진단, 프로브, 사용량 스냅샷)"},"headers":[],"relativePath":"cli/status.md","filePath":"cli/status.md","lastUpdated":null}'),l={name:"cli/status.md"};function n(c,s,o,p,d,h){return e(),t("div",null,[...s[0]||(s[0]=[i(`<h1 id="openclaw-status" tabindex="-1"><code>openclaw status</code> <a class="header-anchor" href="#openclaw-status" aria-label="Permalink to &quot;\`openclaw status\`&quot;">​</a></h1><p>채널 + 세션 진단.</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">openclaw</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> status</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">openclaw</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> status</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --all</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">openclaw</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> status</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --deep</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">openclaw</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> status</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --usage</span></span></code></pre></div><p>참고사항:</p><ul><li><code>--deep</code>은 라이브 프로브를 실행합니다 (WhatsApp Web + Telegram + Discord + Slack + Signal).</li><li><code>--usage</code>는 정규화된 프로바이더 사용 창을 <code>X% left</code>로 출력합니다.</li><li>MiniMax의 원시 <code>usage_percent</code> / <code>usagePercent</code> 필드는 남은 할당량이므로 OpenClaw는 표시 전에 이를 반전합니다; 카운트 기반 필드가 있으면 우선합니다. <code>model_remains</code> 응답은 채팅 모델 항목을 선호하고, 필요할 때 타임스탬프에서 창 레이블을 도출하며, 플랜 레이블에 모델 이름을 포함합니다.</li><li>현재 세션 스냅샷이 희박한 경우 <code>/status</code>는 가장 최근 트랜스크립트 사용 로그에서 토큰 및 캐시 카운터를 채울 수 있습니다. 기존의 0이 아닌 라이브 값은 여전히 트랜스크립트 폴백 값보다 우선합니다.</li><li>트랜스크립트 폴백은 라이브 세션 항목에 활성 런타임 모델 레이블이 누락된 경우에도 복구할 수 있습니다. 해당 트랜스크립트 모델이 선택된 모델과 다르면 상태는 선택된 모델 대신 복구된 런타임 모델에 대해 컨텍스트 창을 확인합니다.</li><li>프롬프트 크기 계산을 위해 트랜스크립트 폴백은 세션 메타데이터가 누락되거나 더 작을 때 더 큰 프롬프트 지향 합계를 선호하므로 커스텀 프로바이더 세션이 <code>0</code> 토큰 표시로 줄어들지 않습니다.</li><li>여러 에이전트가 구성된 경우 출력에는 에이전트별 세션 저장소가 포함됩니다.</li><li>개요에는 사용 가능한 경우 Gateway + 노드 호스트 서비스 설치/런타임 상태가 포함됩니다.</li><li>개요에는 업데이트 채널 + git SHA (소스 체크아웃의 경우)가 포함됩니다.</li><li>업데이트 정보는 개요에 표시됩니다; 업데이트가 사용 가능하면 상태는 <code>openclaw update</code> 실행 힌트를 출력합니다 (<a href="/openclaw-docs-ko/install/updating">업데이트</a> 참조).</li><li>읽기 전용 상태 표면 (<code>status</code>, <code>status --json</code>, <code>status --all</code>)은 가능한 경우 대상 구성 경로에 대해 지원되는 SecretRef를 확인합니다.</li><li>지원되는 채널 SecretRef가 구성되었지만 현재 명령 경로에서 사용할 수 없는 경우 상태는 읽기 전용을 유지하고 충돌하는 대신 저하된 출력을 보고합니다. 사람이 읽을 수 있는 출력은 &quot;이 명령 경로에서 구성된 토큰을 사용할 수 없음&quot; 같은 경고를 표시하고, JSON 출력에는 <code>secretDiagnostics</code>가 포함됩니다.</li><li>명령 로컬 SecretRef 확인이 성공하면 상태는 확인된 스냅샷을 선호하고 최종 출력에서 일시적인 &quot;비밀 사용 불가&quot; 채널 마커를 제거합니다.</li><li><code>status --all</code>에는 비밀 개요 행과 비밀 진단을 요약하는 진단 섹션이 포함됩니다 (가독성을 위해 잘림) 보고서 생성을 중단하지 않습니다.</li></ul>`,5)])])}const k=a(l,[["render",n]]);export{u as __pageData,k as default};
