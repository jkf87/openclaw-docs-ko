import{_ as o,o as a,c as d,ae as c}from"./chunks/framework.C7FLYIpo.js";const u=JSON.parse('{"title":"스트리밍 및 청킹","description":"스트리밍 + 청킹 동작 (블록 응답, 채널 미리보기 스트리밍, 모드 매핑)","frontmatter":{"title":"스트리밍 및 청킹","description":"스트리밍 + 청킹 동작 (블록 응답, 채널 미리보기 스트리밍, 모드 매핑)"},"headers":[],"relativePath":"concepts/streaming.md","filePath":"concepts/streaming.md","lastUpdated":null}'),t={name:"concepts/streaming.md"};function l(n,e,s,i,r,p){return a(),d("div",null,[...e[0]||(e[0]=[c(`<h1 id="스트리밍-청킹" tabindex="-1">스트리밍 + 청킹 <a class="header-anchor" href="#스트리밍-청킹" aria-label="Permalink to &quot;스트리밍 + 청킹&quot;">​</a></h1><p>OpenClaw에는 두 개의 별도 스트리밍 레이어가 있습니다:</p><ul><li><strong>블록 스트리밍 (채널):</strong> 어시스턴트가 작성하면서 완성된 <strong>블록</strong>을 내보냅니다. 이는 일반 채널 메시지입니다 (토큰 델타가 아님).</li><li><strong>미리보기 스트리밍 (Telegram/Discord/Slack):</strong> 생성하는 동안 임시 <strong>미리보기 메시지</strong>를 업데이트합니다.</li></ul><p>현재 채널 메시지에 대한 <strong>진정한 토큰 델타 스트리밍</strong>은 없습니다. 미리보기 스트리밍은 메시지 기반입니다 (전송 + 편집/추가).</p><h2 id="블록-스트리밍-채널-메시지" tabindex="-1">블록 스트리밍 (채널 메시지) <a class="header-anchor" href="#블록-스트리밍-채널-메시지" aria-label="Permalink to &quot;블록 스트리밍 (채널 메시지)&quot;">​</a></h2><p>블록 스트리밍은 어시스턴트 출력이 사용 가능해지면서 거친 청크로 보냅니다.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>모델 출력</span></span>
<span class="line"><span>  └─ text_delta/이벤트</span></span>
<span class="line"><span>       ├─ (blockStreamingBreak=text_end)</span></span>
<span class="line"><span>       │    └─ 버퍼가 증가하면서 청커가 블록을 내보냄</span></span>
<span class="line"><span>       └─ (blockStreamingBreak=message_end)</span></span>
<span class="line"><span>            └─ message_end에서 청커가 플러시</span></span>
<span class="line"><span>                   └─ 채널 전송 (블록 응답)</span></span></code></pre></div><p>범례:</p><ul><li><code>text_delta/이벤트</code>: 모델 스트림 이벤트 (비스트리밍 모델의 경우 희박할 수 있음).</li><li><code>청커</code>: 최소/최대 경계 + 중단 선호도를 적용하는 <code>EmbeddedBlockChunker</code>.</li><li><code>채널 전송</code>: 실제 아웃바운드 메시지 (블록 응답).</li></ul><p><strong>제어:</strong></p><ul><li><code>agents.defaults.blockStreamingDefault</code>: <code>&quot;on&quot;</code>/<code>&quot;off&quot;</code> (기본값 off).</li><li>채널 오버라이드: <code>*.blockStreaming</code> (및 계정별 변형)으로 채널별 <code>&quot;on&quot;</code>/<code>&quot;off&quot;</code> 강제.</li><li><code>agents.defaults.blockStreamingBreak</code>: <code>&quot;text_end&quot;</code> 또는 <code>&quot;message_end&quot;</code>.</li><li><code>agents.defaults.blockStreamingChunk</code>: <code>{ minChars, maxChars, breakPreference? }</code>.</li><li><code>agents.defaults.blockStreamingCoalesce</code>: <code>{ minChars?, maxChars?, idleMs? }</code> (전송 전 스트리밍된 블록 병합).</li><li>채널 하드 상한: <code>*.textChunkLimit</code> (예: <code>channels.whatsapp.textChunkLimit</code>).</li><li>채널 청크 모드: <code>*.chunkMode</code> (<code>length</code> 기본값, <code>newline</code>은 길이 청킹 전에 빈 줄 (단락 경계)에서 분리).</li><li>Discord 소프트 상한: <code>channels.discord.maxLinesPerMessage</code> (기본값 17)은 UI 클리핑을 피하기 위해 긴 응답을 분리.</li></ul><p><strong>경계 의미론:</strong></p><ul><li><code>text_end</code>: 청커가 내보내는 즉시 스트림 블록; 각 <code>text_end</code>에서 플러시.</li><li><code>message_end</code>: 어시스턴트 메시지가 완료될 때까지 기다린 다음 버퍼된 출력 플러시.</li></ul><p><code>message_end</code>는 버퍼된 텍스트가 <code>maxChars</code>를 초과하면 여전히 청커를 사용하므로 끝에서 여러 청크를 내보낼 수 있습니다.</p><h2 id="청킹-알고리즘-낮음-높음-경계" tabindex="-1">청킹 알고리즘 (낮음/높음 경계) <a class="header-anchor" href="#청킹-알고리즘-낮음-높음-경계" aria-label="Permalink to &quot;청킹 알고리즘 (낮음/높음 경계)&quot;">​</a></h2><p>블록 청킹은 <code>EmbeddedBlockChunker</code>로 구현됩니다:</p><ul><li><strong>낮은 경계:</strong> 버퍼가 <code>minChars</code> 이상이 될 때까지 내보내지 않습니다 (강제가 아닌 경우).</li><li><strong>높은 경계:</strong> <code>maxChars</code> 전에 분리를 선호합니다. 강제된 경우 <code>maxChars</code>에서 분리.</li><li><strong>중단 선호도:</strong> <code>paragraph</code> → <code>newline</code> → <code>sentence</code> → <code>whitespace</code> → 하드 중단.</li><li><strong>코드 펜스:</strong> 펜스 내부에서 절대 분리하지 않습니다. <code>maxChars</code>에서 강제된 경우, 펜스를 닫고 다시 열어 Markdown을 유효하게 유지합니다.</li></ul><p><code>maxChars</code>는 채널 <code>textChunkLimit</code>으로 제한되므로 채널별 상한을 초과할 수 없습니다.</p><h2 id="통합-스트리밍된-블록-병합" tabindex="-1">통합 (스트리밍된 블록 병합) <a class="header-anchor" href="#통합-스트리밍된-블록-병합" aria-label="Permalink to &quot;통합 (스트리밍된 블록 병합)&quot;">​</a></h2><p>블록 스트리밍이 활성화되면 OpenClaw는 <strong>연속 블록 청크</strong>를 전송하기 전에 <strong>병합</strong>할 수 있습니다. 이는 여전히 진행형 출력을 제공하면서 &quot;단일 라인 스팸&quot;을 줄입니다.</p><ul><li>통합은 플러시 전에 <strong>유휴 간격</strong> (<code>idleMs</code>)을 기다립니다.</li><li>버퍼는 <code>maxChars</code>로 제한되며 초과하면 플러시됩니다.</li><li><code>minChars</code>는 충분한 텍스트가 누적될 때까지 작은 단편이 전송되지 않도록 합니다 (최종 플러시는 항상 나머지 텍스트를 보냄).</li><li>조이너는 <code>blockStreamingChunk.breakPreference</code>에서 파생됩니다 (<code>paragraph</code> → <code>\\n\\n</code>, <code>newline</code> → <code>\\n</code>, <code>sentence</code> → 공백).</li><li>채널 오버라이드는 <code>*.blockStreamingCoalesce</code>를 통해 사용 가능합니다 (계정별 구성 포함).</li><li>기본 통합 <code>minChars</code>는 오버라이드되지 않는 한 Signal/Slack/Discord에 대해 1500으로 증가됩니다.</li></ul><h2 id="블록-간-인간적-페이싱" tabindex="-1">블록 간 인간적 페이싱 <a class="header-anchor" href="#블록-간-인간적-페이싱" aria-label="Permalink to &quot;블록 간 인간적 페이싱&quot;">​</a></h2><p>블록 스트리밍이 활성화된 경우, 블록 응답 사이에 <strong>무작위화된 일시 정지</strong>를 추가할 수 있습니다 (첫 번째 블록 이후). 이는 멀티 버블 응답을 더 자연스럽게 느끼게 합니다.</p><ul><li>구성: <code>agents.defaults.humanDelay</code> (<code>agents.list[].humanDelay</code>를 통해 에이전트별 오버라이드).</li><li>모드: <code>off</code> (기본값), <code>natural</code> (800~2500ms), <code>custom</code> (<code>minMs</code>/<code>maxMs</code>).</li><li><strong>블록 응답</strong>에만 적용됩니다. 최종 응답이나 도구 요약에는 적용되지 않습니다.</li></ul><h2 id="청크-스트리밍-또는-전체" tabindex="-1">&quot;청크 스트리밍 또는 전체&quot; <a class="header-anchor" href="#청크-스트리밍-또는-전체" aria-label="Permalink to &quot;&quot;청크 스트리밍 또는 전체&quot;&quot;">​</a></h2><p>다음에 매핑됩니다:</p><ul><li><strong>청크 스트리밍:</strong> <code>blockStreamingDefault: &quot;on&quot;</code> + <code>blockStreamingBreak: &quot;text_end&quot;</code> (진행하면서 내보냄). 비Telegram 채널은 <code>*.blockStreaming: true</code>도 필요합니다.</li><li><strong>끝에서 전체 스트리밍:</strong> <code>blockStreamingBreak: &quot;message_end&quot;</code> (한 번 플러시, 매우 길면 여러 청크 가능).</li><li><strong>블록 스트리밍 없음:</strong> <code>blockStreamingDefault: &quot;off&quot;</code> (최종 응답만).</li></ul><p><strong>채널 참고:</strong> 블록 스트리밍은 <code>*.blockStreaming</code>이 명시적으로 <code>true</code>로 설정되지 않으면 <strong>꺼져 있습니다</strong>. 채널은 블록 응답 없이 라이브 미리보기를 스트리밍할 수 있습니다 (<code>channels.&amp;lt;channel&amp;gt;.streaming</code>).</p><p>구성 위치 참고: <code>blockStreaming*</code> 기본값은 루트 구성이 아닌 <code>agents.defaults</code> 아래에 있습니다.</p><h2 id="미리보기-스트리밍-모드" tabindex="-1">미리보기 스트리밍 모드 <a class="header-anchor" href="#미리보기-스트리밍-모드" aria-label="Permalink to &quot;미리보기 스트리밍 모드&quot;">​</a></h2><p>정식 키: <code>channels.&amp;lt;channel&amp;gt;.streaming</code></p><p>모드:</p><ul><li><code>off</code>: 미리보기 스트리밍 비활성화.</li><li><code>partial</code>: 최신 텍스트로 대체되는 단일 미리보기.</li><li><code>block</code>: 청크/추가 단계로 업데이트되는 미리보기.</li><li><code>progress</code>: 생성 중 진행/상태 미리보기, 완료 시 최종 응답.</li></ul><h3 id="채널-매핑" tabindex="-1">채널 매핑 <a class="header-anchor" href="#채널-매핑" aria-label="Permalink to &quot;채널 매핑&quot;">​</a></h3><table tabindex="0"><thead><tr><th>채널</th><th><code>off</code></th><th><code>partial</code></th><th><code>block</code></th><th><code>progress</code></th></tr></thead><tbody><tr><td>Telegram</td><td>✅</td><td>✅</td><td>✅</td><td><code>partial</code>로 매핑</td></tr><tr><td>Discord</td><td>✅</td><td>✅</td><td>✅</td><td><code>partial</code>로 매핑</td></tr><tr><td>Slack</td><td>✅</td><td>✅</td><td>✅</td><td>✅</td></tr></tbody></table><p>Slack 전용:</p><ul><li><code>channels.slack.streaming.nativeTransport</code>는 <code>channels.slack.streaming.mode=&quot;partial&quot;</code> (기본값: <code>true</code>)일 때 Slack 네이티브 스트리밍 API 호출을 토글합니다.</li><li>Slack 네이티브 스트리밍 및 Slack 어시스턴트 스레드 상태는 응답 스레드 대상이 필요합니다. 최상위 DM은 그 스레드 스타일 미리보기를 표시하지 않습니다.</li></ul><p>레거시 키 마이그레이션:</p><ul><li>Telegram: <code>streamMode</code> + 불리언 <code>streaming</code>은 <code>streaming</code> 열거형으로 자동 마이그레이션.</li><li>Discord: <code>streamMode</code> + 불리언 <code>streaming</code>은 <code>streaming</code> 열거형으로 자동 마이그레이션.</li><li>Slack: <code>streamMode</code>는 <code>streaming.mode</code>로 자동 마이그레이션. 불리언 <code>streaming</code>은 <code>streaming.mode</code> + <code>streaming.nativeTransport</code>로 자동 마이그레이션. 레거시 <code>nativeStreaming</code>은 <code>streaming.nativeTransport</code>로 자동 마이그레이션.</li></ul><h3 id="런타임-동작" tabindex="-1">런타임 동작 <a class="header-anchor" href="#런타임-동작" aria-label="Permalink to &quot;런타임 동작&quot;">​</a></h3><p>Telegram:</p><ul><li>DM 및 그룹/토픽에서 <code>sendMessage</code> + <code>editMessageText</code> 미리보기 업데이트를 사용합니다.</li><li>Telegram 블록 스트리밍이 명시적으로 활성화된 경우 미리보기 스트리밍을 건너뜁니다 (이중 스트리밍 방지).</li><li><code>/reasoning stream</code>은 추론을 미리보기에 쓸 수 있습니다.</li></ul><p>Discord:</p><ul><li>전송 + 편집 미리보기 메시지를 사용합니다.</li><li><code>block</code> 모드는 초안 청킹 (<code>draftChunk</code>)을 사용합니다.</li><li>Discord 블록 스트리밍이 명시적으로 활성화된 경우 미리보기 스트리밍을 건너뜁니다.</li></ul><p>Slack:</p><ul><li><code>partial</code>은 사용 가능한 경우 Slack 네이티브 스트리밍 (<code>chat.startStream</code>/<code>append</code>/<code>stop</code>)을 사용할 수 있습니다.</li><li><code>block</code>은 추가 스타일 초안 미리보기를 사용합니다.</li><li><code>progress</code>는 상태 미리보기 텍스트를 사용한 다음 최종 응답을 보냅니다.</li></ul><h2 id="관련-항목" tabindex="-1">관련 항목 <a class="header-anchor" href="#관련-항목" aria-label="Permalink to &quot;관련 항목&quot;">​</a></h2><ul><li><a href="/openclaw-docs-ko/concepts/messages">메시지</a> — 메시지 라이프사이클 및 전달</li><li><a href="/openclaw-docs-ko/concepts/retry">재시도</a> — 전달 실패 시 재시도 동작</li><li><a href="/openclaw-docs-ko/channels">채널</a> — 채널별 스트리밍 지원</li></ul>`,48)])])}const h=o(t,[["render",l]]);export{u as __pageData,h as default};
