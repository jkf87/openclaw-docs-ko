import{_ as o,o as l,c as a,ag as i}from"./chunks/framework.CTpQozEL.js";const u=JSON.parse('{"title":"모델 장애 조치","description":"OpenClaw가 인증 프로파일을 로테이션하고 모델 간에 폴백하는 방법","frontmatter":{"title":"모델 장애 조치","description":"OpenClaw가 인증 프로파일을 로테이션하고 모델 간에 폴백하는 방법"},"headers":[],"relativePath":"concepts/model-failover.md","filePath":"concepts/model-failover.md","lastUpdated":null}'),t={name:"concepts/model-failover.md"};function s(d,e,n,r,c,p){return l(),a("div",null,[...e[0]||(e[0]=[i(`<h1 id="모델-장애-조치" tabindex="-1">모델 장애 조치 <a class="header-anchor" href="#모델-장애-조치" aria-label="Permalink to &quot;모델 장애 조치&quot;">​</a></h1><p>OpenClaw는 두 단계로 장애를 처리합니다:</p><ol><li>현재 프로바이더 내에서 <strong>인증 프로파일 로테이션</strong>.</li><li><code>agents.defaults.model.fallbacks</code>의 다음 모델로 <strong>모델 폴백</strong>.</li></ol><p>이 문서는 런타임 규칙과 이를 뒷받침하는 데이터를 설명합니다.</p><h2 id="런타임-흐름" tabindex="-1">런타임 흐름 <a class="header-anchor" href="#런타임-흐름" aria-label="Permalink to &quot;런타임 흐름&quot;">​</a></h2><p>일반 텍스트 실행의 경우, OpenClaw는 다음 순서로 후보를 평가합니다:</p><ol><li>현재 선택된 세션 모델.</li><li>순서대로 구성된 <code>agents.defaults.model.fallbacks</code>.</li><li>실행이 오버라이드로 시작된 경우 마지막에 구성된 기본 모델.</li></ol><p>각 후보 내에서 OpenClaw는 다음 모델 후보로 진행하기 전에 인증 프로파일 장애 조치를 시도합니다.</p><p>상위 수준 순서:</p><ol><li>활성 세션 모델 및 인증 프로파일 선호도를 확인합니다.</li><li>모델 후보 체인을 빌드합니다.</li><li>인증 프로파일 로테이션/쿨다운 규칙으로 현재 프로바이더를 시도합니다.</li><li>해당 프로바이더가 장애 조치에 해당하는 오류로 소진되면 다음 모델 후보로 이동합니다.</li><li>재시도가 시작되기 전에 선택된 폴백 오버라이드를 영속화하여 다른 세션 리더가 실행자가 사용할 프로바이더/모델을 볼 수 있게 합니다.</li><li>폴백 후보가 실패하면 해당 실패한 후보와 일치하는 경우에만 폴백 소유 세션 오버라이드 필드를 롤백합니다.</li><li>모든 후보가 실패하면 쿨다운 만료가 알려진 경우 가장 빠른 쿨다운 만료와 함께 시도별 세부사항이 포함된 <code>FallbackSummaryError</code>를 발생시킵니다.</li></ol><p>이는 의도적으로 &quot;전체 세션을 저장 및 복원&quot;보다 좁습니다. 응답 실행자는 폴백을 위해 소유하는 모델 선택 필드만 영속화합니다:</p><ul><li><code>providerOverride</code></li><li><code>modelOverride</code></li><li><code>authProfileOverride</code></li><li><code>authProfileOverrideSource</code></li><li><code>authProfileOverrideCompactionCount</code></li></ul><p>이는 실패한 폴백 재시도가 실행이 진행되는 동안 발생한 수동 <code>/model</code> 변경이나 세션 로테이션 업데이트 같은 최신의 관련 없는 세션 변형을 덮어쓰는 것을 방지합니다.</p><h2 id="인증-저장소-키-oauth" tabindex="-1">인증 저장소 (키 + OAuth) <a class="header-anchor" href="#인증-저장소-키-oauth" aria-label="Permalink to &quot;인증 저장소 (키 + OAuth)&quot;">​</a></h2><p>OpenClaw는 API 키와 OAuth 토큰 모두에 <strong>인증 프로파일</strong>을 사용합니다.</p><ul><li>시크릿은 <code>~/.openclaw/agents/&amp;lt;agentId&amp;gt;/agent/auth-profiles.json</code> (레거시: <code>~/.openclaw/agent/auth-profiles.json</code>)에 저장됩니다.</li><li>런타임 인증 라우팅 상태는 <code>~/.openclaw/agents/&amp;lt;agentId&amp;gt;/agent/auth-state.json</code>에 있습니다.</li><li>구성 <code>auth.profiles</code> / <code>auth.order</code>는 <strong>메타데이터 + 라우팅만</strong> (시크릿 없음).</li><li>레거시 가져오기 전용 OAuth 파일: <code>~/.openclaw/credentials/oauth.json</code> (첫 사용 시 <code>auth-profiles.json</code>에 가져옴).</li></ul><p>자세한 내용: <a href="/openclaw-docs-ko/concepts/oauth">/concepts/oauth</a></p><p>자격 증명 유형:</p><ul><li><code>type: &quot;api_key&quot;</code> → <code>{ provider, key }</code></li><li><code>type: &quot;oauth&quot;</code> → <code>{ provider, access, refresh, expires, email? }</code> (일부 프로바이더에서 <code>projectId</code>/<code>enterpriseUrl</code> 포함)</li></ul><h2 id="프로파일-id" tabindex="-1">프로파일 ID <a class="header-anchor" href="#프로파일-id" aria-label="Permalink to &quot;프로파일 ID&quot;">​</a></h2><p>OAuth 로그인은 여러 계정이 공존할 수 있도록 고유한 프로파일을 생성합니다.</p><ul><li>기본값: 이메일을 사용할 수 없는 경우 <code>provider:default</code>.</li><li>이메일이 있는 OAuth: <code>provider:&amp;lt;email&amp;gt;</code> (예: <code>google-antigravity:user@gmail.com</code>).</li></ul><p>프로파일은 <code>profiles</code> 아래의 <code>~/.openclaw/agents/&amp;lt;agentId&amp;gt;/agent/auth-profiles.json</code>에 있습니다.</p><h2 id="로테이션-순서" tabindex="-1">로테이션 순서 <a class="header-anchor" href="#로테이션-순서" aria-label="Permalink to &quot;로테이션 순서&quot;">​</a></h2><p>프로바이더에 여러 프로파일이 있는 경우, OpenClaw는 다음과 같이 순서를 선택합니다:</p><ol><li><strong>명시적 구성</strong>: <code>auth.order[provider]</code> (설정된 경우).</li><li><strong>구성된 프로파일</strong>: 프로바이더로 필터링된 <code>auth.profiles</code>.</li><li><strong>저장된 프로파일</strong>: 프로바이더의 <code>auth-profiles.json</code> 항목.</li></ol><p>명시적 순서가 구성되지 않은 경우, OpenClaw는 라운드 로빈 순서를 사용합니다:</p><ul><li><strong>기본 키:</strong> 프로파일 유형 (<strong>API 키 전에 OAuth</strong>).</li><li><strong>보조 키:</strong> <code>usageStats.lastUsed</code> (각 유형 내에서 가장 오래된 것 우선).</li><li><strong>쿨다운/비활성화된 프로파일</strong>은 가장 빠른 만료 순으로 끝으로 이동됩니다.</li></ul><h3 id="세션-고정-캐시-친화적" tabindex="-1">세션 고정 (캐시 친화적) <a class="header-anchor" href="#세션-고정-캐시-친화적" aria-label="Permalink to &quot;세션 고정 (캐시 친화적)&quot;">​</a></h3><p>OpenClaw는 프로바이더 캐시를 따뜻하게 유지하기 위해 <strong>세션당 선택된 인증 프로파일을 고정</strong>합니다. 모든 요청에서 로테이션하지 않습니다. 고정된 프로파일은 다음 조건이 될 때까지 재사용됩니다:</p><ul><li>세션이 재설정됨 (<code>/new</code> / <code>/reset</code>)</li><li>컴팩션이 완료됨 (컴팩션 수가 증가함)</li><li>프로파일이 쿨다운/비활성화 상태</li></ul><p><code>/model …@&amp;lt;profileId&amp;gt;</code>를 통한 수동 선택은 해당 세션에 대한 <strong>사용자 오버라이드</strong>를 설정하며, 새 세션이 시작될 때까지 자동 로테이션되지 않습니다.</p><p>자동 고정된 프로파일 (세션 라우터가 선택)은 <strong>선호도</strong>로 처리됩니다: 먼저 시도되지만 OpenClaw는 속도 제한/타임아웃 시 다른 프로파일로 로테이션할 수 있습니다. 사용자 고정 프로파일은 해당 프로파일에 잠깁니다; 실패하고 모델 폴백이 구성된 경우, OpenClaw는 프로파일을 전환하는 대신 다음 모델로 이동합니다.</p><h3 id="oauth가-사라진-것처럼-보이는-이유" tabindex="-1">OAuth가 &quot;사라진 것처럼 보이는&quot; 이유 <a class="header-anchor" href="#oauth가-사라진-것처럼-보이는-이유" aria-label="Permalink to &quot;OAuth가 &quot;사라진 것처럼 보이는&quot; 이유&quot;">​</a></h3><p>동일한 프로바이더에 대해 OAuth 프로파일과 API 키 프로파일이 모두 있는 경우, 고정하지 않으면 라운드 로빈이 메시지 간에 전환할 수 있습니다. 단일 프로파일을 강제하려면:</p><ul><li><code>auth.order[provider] = [&quot;provider:profileId&quot;]</code>로 고정하거나,</li><li>프로파일 오버라이드로 <code>/model …</code>을 통한 세션별 오버라이드를 사용하십시오 (UI/채팅 서피스에서 지원하는 경우).</li></ul><h2 id="쿨다운" tabindex="-1">쿨다운 <a class="header-anchor" href="#쿨다운" aria-label="Permalink to &quot;쿨다운&quot;">​</a></h2><p>인증/속도 제한 오류 (또는 속도 제한처럼 보이는 타임아웃)로 인해 프로파일이 실패하면 OpenClaw는 이를 쿨다운으로 표시하고 다음 프로파일로 이동합니다. 해당 속도 제한 버킷은 일반 <code>429</code>보다 넓습니다: <code>Too many concurrent requests</code>, <code>ThrottlingException</code>, <code>concurrency limit reached</code>, <code>workers_ai ... quota limit exceeded</code>, <code>throttled</code>, <code>resource exhausted</code>, <code>weekly/monthly limit reached</code> 같은 프로바이더 메시지도 포함합니다. 형식/잘못된 요청 오류 (예: Cloud Code Assist 도구 호출 ID 검증 실패)는 장애 조치에 해당하며 동일한 쿨다운을 사용합니다. <code>Unhandled stop reason: error</code>, <code>stop reason: error</code>, <code>reason: error</code> 같은 OpenAI 호환 중지 이유 오류는 타임아웃/장애 조치 신호로 분류됩니다.</p><p>속도 제한 쿨다운은 모델 범위로 지정될 수 있습니다:</p><ul><li>OpenClaw는 실패하는 모델 ID가 알려진 경우 속도 제한 실패에 대해 <code>cooldownModel</code>을 기록합니다.</li><li>동일한 프로바이더의 형제 모델은 쿨다운이 다른 모델로 범위가 지정된 경우 여전히 시도할 수 있습니다.</li><li>청구/비활성화 윈도우는 여전히 모델 전체에 걸쳐 전체 프로파일을 차단합니다.</li></ul><p>쿨다운은 지수적 백오프를 사용합니다:</p><ul><li>1분</li><li>5분</li><li>25분</li><li>1시간 (상한)</li></ul><p>상태는 <code>usageStats</code> 아래의 <code>auth-state.json</code>에 저장됩니다:</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;usageStats&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;provider:profile&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;lastUsed&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1736160000000</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;cooldownUntil&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1736160600000</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;errorCount&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><h2 id="청구-비활성화" tabindex="-1">청구 비활성화 <a class="header-anchor" href="#청구-비활성화" aria-label="Permalink to &quot;청구 비활성화&quot;">​</a></h2><p>청구/크레딧 실패 (예: &quot;insufficient credits&quot; / &quot;credit balance too low&quot;)는 장애 조치에 해당하지만, 일반적으로 일시적이지 않습니다. 짧은 쿨다운 대신 OpenClaw는 프로파일을 <strong>비활성화</strong> (더 긴 백오프로)로 표시하고 다음 프로파일/프로바이더로 로테이션합니다.</p><p>쿨다운은 지수적 백오프를 사용합니다:</p><ul><li>청구 백오프는 <strong>5시간</strong>으로 시작하고, 청구 실패당 두 배가 되며, <strong>24시간</strong>에 상한이 있습니다.</li><li>백오프 카운터는 프로파일이 <strong>24시간</strong> 동안 실패하지 않으면 재설정됩니다 (구성 가능).</li><li>과부하 재시도는 모델 폴백 전에 <strong>1번의 동일 프로바이더 프로파일 로테이션</strong>을 허용합니다.</li><li>과부하 재시도는 기본적으로 <strong>0ms 백오프</strong>를 사용합니다.</li></ul><h2 id="모델-폴백" tabindex="-1">모델 폴백 <a class="header-anchor" href="#모델-폴백" aria-label="Permalink to &quot;모델 폴백&quot;">​</a></h2><p>프로바이더의 모든 프로파일이 실패하면 OpenClaw는 <code>agents.defaults.model.fallbacks</code>의 다음 모델로 이동합니다. 이는 인증 실패, 속도 제한, 프로파일 로테이션을 소진한 타임아웃에 적용됩니다 (다른 오류는 폴백을 진행하지 않습니다).</p><p>과부하 및 속도 제한 오류는 청구 쿨다운보다 더 공격적으로 처리됩니다. 기본적으로 OpenClaw는 하나의 동일 프로바이더 인증 프로파일 재시도를 허용한 다음 기다리지 않고 다음 구성된 모델 폴백으로 전환합니다. <code>auth.cooldowns.overloadedProfileRotations</code>, <code>auth.cooldowns.overloadedBackoffMs</code>, <code>auth.cooldowns.rateLimitedProfileRotations</code>로 조정하십시오.</p><p>실행이 모델 오버라이드 (후크 또는 CLI)로 시작된 경우, 폴백은 구성된 폴백을 시도한 후 여전히 <code>agents.defaults.model.primary</code>에서 끝납니다.</p><h3 id="후보-체인-규칙" tabindex="-1">후보 체인 규칙 <a class="header-anchor" href="#후보-체인-규칙" aria-label="Permalink to &quot;후보 체인 규칙&quot;">​</a></h3><p>OpenClaw는 현재 요청된 <code>provider/model</code> 및 구성된 폴백에서 후보 목록을 빌드합니다.</p><p>규칙:</p><ul><li>요청된 모델이 항상 첫 번째입니다.</li><li>명시적 구성된 폴백은 중복 제거되지만 모델 허용 목록으로 필터링되지 않습니다. 명시적 오퍼레이터 의도로 처리됩니다.</li><li>현재 실행이 동일한 프로바이더 계열의 구성된 폴백에 이미 있는 경우, OpenClaw는 전체 구성된 체인을 계속 사용합니다.</li><li>현재 실행이 구성과 다른 프로바이더에 있고 해당 현재 모델이 이미 구성된 폴백 체인의 일부가 아닌 경우, OpenClaw는 다른 프로바이더의 관련 없는 구성된 폴백을 추가하지 않습니다.</li><li>실행이 오버라이드로 시작된 경우, 구성된 기본 모델이 끝에 추가되어 이전 후보가 소진되면 체인이 일반 기본값으로 다시 안정화될 수 있습니다.</li></ul><h3 id="폴백을-진행하는-오류" tabindex="-1">폴백을 진행하는 오류 <a class="header-anchor" href="#폴백을-진행하는-오류" aria-label="Permalink to &quot;폴백을 진행하는 오류&quot;">​</a></h3><p>모델 폴백은 다음에서 계속됩니다:</p><ul><li>인증 실패</li><li>속도 제한 및 쿨다운 소진</li><li>과부하/프로바이더 바쁨 오류</li><li>타임아웃 형태의 장애 조치 오류</li><li>청구 비활성화</li><li><code>LiveSessionModelSwitchError</code> (낡은 영속화된 모델이 외부 재시도 루프를 생성하지 않도록 장애 조치 경로로 정규화됨)</li><li>남은 후보가 있을 때 다른 인식되지 않는 오류</li></ul><p>모델 폴백은 다음에서 계속되지 않습니다:</p><ul><li>타임아웃/장애 조치 형태가 아닌 명시적 중단</li><li>컴팩션/재시도 로직 내에 유지되어야 하는 컨텍스트 오버플로우 오류</li><li>후보가 남지 않았을 때 최종 알 수 없는 오류</li></ul><h2 id="관찰-가능성-및-실패-요약" tabindex="-1">관찰 가능성 및 실패 요약 <a class="header-anchor" href="#관찰-가능성-및-실패-요약" aria-label="Permalink to &quot;관찰 가능성 및 실패 요약&quot;">​</a></h2><p><code>runWithModelFallback(...)</code>은 로그와 사용자 대면 쿨다운 메시지를 공급하는 시도별 세부사항을 기록합니다:</p><ul><li>시도된 프로바이더/모델</li><li>이유 (<code>rate_limit</code>, <code>overloaded</code>, <code>billing</code>, <code>auth</code>, <code>model_not_found</code> 및 유사한 장애 조치 이유)</li><li>선택적 상태/코드</li><li>사람이 읽을 수 있는 오류 요약</li></ul><p>모든 후보가 실패하면 OpenClaw는 <code>FallbackSummaryError</code>를 발생시킵니다. 외부 응답 실행자는 이를 사용하여 &quot;모든 모델이 일시적으로 속도 제한됨&quot; 같은 더 구체적인 메시지를 빌드하고 알려진 경우 가장 빠른 쿨다운 만료를 포함할 수 있습니다.</p><h2 id="관련-구성" tabindex="-1">관련 구성 <a class="header-anchor" href="#관련-구성" aria-label="Permalink to &quot;관련 구성&quot;">​</a></h2><p><a href="/openclaw-docs-ko/gateway/configuration">게이트웨이 구성</a>을 참조하십시오:</p><ul><li><code>auth.profiles</code> / <code>auth.order</code></li><li><code>auth.cooldowns.billingBackoffHours</code> / <code>auth.cooldowns.billingBackoffHoursByProvider</code></li><li><code>auth.cooldowns.billingMaxHours</code> / <code>auth.cooldowns.failureWindowHours</code></li><li><code>auth.cooldowns.overloadedProfileRotations</code> / <code>auth.cooldowns.overloadedBackoffMs</code></li><li><code>auth.cooldowns.rateLimitedProfileRotations</code></li><li><code>agents.defaults.model.primary</code> / <code>agents.defaults.model.fallbacks</code></li><li><code>agents.defaults.imageModel</code> 라우팅</li></ul><p>더 넓은 모델 선택 및 폴백 개요는 <a href="/openclaw-docs-ko/concepts/models">모델</a>을 참조하십시오.</p>`,69)])])}const g=o(t,[["render",s]]);export{u as __pageData,g as default};
