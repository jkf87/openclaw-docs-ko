import{_ as o,o as a,c as t,ag as d}from"./chunks/framework.CTpQozEL.js";const u=JSON.parse('{"title":"스트리밍과 청킹","description":"스트리밍 + 청킹 동작(블록 응답, 채널 preview 스트리밍, 모드 매핑)","frontmatter":{"title":"스트리밍과 청킹","description":"스트리밍 + 청킹 동작(블록 응답, 채널 preview 스트리밍, 모드 매핑)"},"headers":[],"relativePath":"concepts/streaming.md","filePath":"concepts/streaming.md","lastUpdated":null}'),c={name:"concepts/streaming.md"};function l(r,e,i,n,s,p){return a(),t("div",null,[...e[0]||(e[0]=[d(`<h1 id="스트리밍-청킹" tabindex="-1">스트리밍 + 청킹 <a class="header-anchor" href="#스트리밍-청킹" aria-label="Permalink to &quot;스트리밍 + 청킹&quot;">​</a></h1><p>OpenClaw에는 두 개의 별도 스트리밍 레이어가 있습니다:</p><ul><li><strong>블록 스트리밍 (채널용):</strong> assistant가 작성하는 동안 완성된 <strong>블록(block)</strong> 을 내보냅니다. 이는 일반적인 채널 메시지입니다(token delta가 아님).</li><li><strong>Preview 스트리밍 (Telegram/Discord/Slack):</strong> 생성 중에 임시 <strong>preview 메시지</strong>를 업데이트합니다.</li></ul><p>오늘날 채널 메시지에 대한 <strong>진정한 token-delta 스트리밍은 없습니다.</strong> Preview 스트리밍은 메시지 기반입니다(전송 + 편집/추가).</p><h2 id="블록-스트리밍-채널-메시지" tabindex="-1">블록 스트리밍 (채널 메시지) <a class="header-anchor" href="#블록-스트리밍-채널-메시지" aria-label="Permalink to &quot;블록 스트리밍 (채널 메시지)&quot;">​</a></h2><p>블록 스트리밍은 assistant 출력이 가용해지는 대로 거친(coarse) 청크 단위로 전송합니다.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Model output</span></span>
<span class="line"><span>  └─ text_delta/events</span></span>
<span class="line"><span>       ├─ (blockStreamingBreak=text_end)</span></span>
<span class="line"><span>       │    └─ chunker emits blocks as buffer grows</span></span>
<span class="line"><span>       └─ (blockStreamingBreak=message_end)</span></span>
<span class="line"><span>            └─ chunker flushes at message_end</span></span>
<span class="line"><span>                   └─ channel send (block replies)</span></span></code></pre></div><p>범례:</p><ul><li><code>text_delta/events</code>: 모델 스트림 이벤트(비스트리밍 모델의 경우 희박할 수 있음).</li><li><code>chunker</code>: min/max 경계 + break preference를 적용하는 <code>EmbeddedBlockChunker</code>.</li><li><code>channel send</code>: 실제 아웃바운드 메시지(블록 응답).</li></ul><p><strong>제어 값:</strong></p><ul><li><code>agents.defaults.blockStreamingDefault</code>: <code>&quot;on&quot;</code>/<code>&quot;off&quot;</code> (기본값 off).</li><li>채널 오버라이드: <code>*.blockStreaming</code>(및 계정별 변형)으로 채널별 <code>&quot;on&quot;</code>/<code>&quot;off&quot;</code> 강제.</li><li><code>agents.defaults.blockStreamingBreak</code>: <code>&quot;text_end&quot;</code> 또는 <code>&quot;message_end&quot;</code>.</li><li><code>agents.defaults.blockStreamingChunk</code>: <code>{ minChars, maxChars, breakPreference? }</code>.</li><li><code>agents.defaults.blockStreamingCoalesce</code>: <code>{ minChars?, maxChars?, idleMs? }</code> (전송 전에 스트리밍된 블록 병합).</li><li>채널 하드 캡(hard cap): <code>*.textChunkLimit</code> (예: <code>channels.whatsapp.textChunkLimit</code>).</li><li>채널 청크 모드: <code>*.chunkMode</code> (<code>length</code>가 기본값, <code>newline</code>은 length 청킹 전에 빈 줄(문단 경계)에서 분할).</li><li>Discord soft cap: <code>channels.discord.maxLinesPerMessage</code> (기본값 17) — UI 잘림을 피하기 위해 세로로 긴 응답을 분할.</li></ul><p><strong>경계 의미론:</strong></p><ul><li><code>text_end</code>: chunker가 내보내는 대로 블록을 스트리밍하며, 각 <code>text_end</code>마다 플러시합니다.</li><li><code>message_end</code>: assistant 메시지가 끝날 때까지 대기한 후 버퍼링된 출력을 플러시합니다.</li></ul><p><code>message_end</code>도 버퍼링된 텍스트가 <code>maxChars</code>를 초과하면 chunker를 사용하므로, 끝에서 여러 청크를 내보낼 수 있습니다.</p><h2 id="청킹-알고리즘-low-high-경계" tabindex="-1">청킹 알고리즘 (low/high 경계) <a class="header-anchor" href="#청킹-알고리즘-low-high-경계" aria-label="Permalink to &quot;청킹 알고리즘 (low/high 경계)&quot;">​</a></h2><p>블록 청킹은 <code>EmbeddedBlockChunker</code>로 구현됩니다:</p><ul><li><strong>Low bound:</strong> (강제되지 않는 한) 버퍼가 <code>minChars</code> 이상이 될 때까지 내보내지 않음.</li><li><strong>High bound:</strong> <code>maxChars</code> 이전에서 분할 선호; 강제되면 <code>maxChars</code>에서 분할.</li><li><strong>Break preference:</strong> <code>paragraph</code> → <code>newline</code> → <code>sentence</code> → <code>whitespace</code> → hard break.</li><li><strong>Code fence:</strong> fence 내부에서는 절대 분할하지 않음; <code>maxChars</code>에서 강제 분할될 때는 Markdown을 유효하게 유지하기 위해 fence를 닫고 다시 엽니다.</li></ul><p><code>maxChars</code>는 채널의 <code>textChunkLimit</code>에 clamp되므로 채널별 캡을 초과할 수 없습니다.</p><h2 id="병합-coalescing-—-스트리밍된-블록-합치기" tabindex="-1">병합 (Coalescing) — 스트리밍된 블록 합치기 <a class="header-anchor" href="#병합-coalescing-—-스트리밍된-블록-합치기" aria-label="Permalink to &quot;병합 (Coalescing) — 스트리밍된 블록 합치기&quot;">​</a></h2><p>블록 스트리밍이 활성화되면, OpenClaw는 전송 전에 <strong>연속된 블록 청크들을 병합</strong>할 수 있습니다. 이는 점진적인 출력을 제공하면서도 &quot;한 줄 스팸&quot;을 줄입니다.</p><ul><li>Coalescing은 플러시 전에 <strong>idle gap</strong>(<code>idleMs</code>)을 기다립니다.</li><li>버퍼는 <code>maxChars</code>로 상한이 정해지며, 이를 초과하면 플러시됩니다.</li><li><code>minChars</code>는 충분한 텍스트가 모이기 전까지 작은 단편의 전송을 막습니다 (최종 플러시는 항상 남은 텍스트를 전송).</li><li>Joiner는 <code>blockStreamingChunk.breakPreference</code>에서 파생됩니다 (<code>paragraph</code> → <code>\\n\\n</code>, <code>newline</code> → <code>\\n</code>, <code>sentence</code> → space).</li><li>채널 오버라이드는 <code>*.blockStreamingCoalesce</code>(계정별 설정 포함)를 통해 제공됩니다.</li><li>Signal/Slack/Discord의 기본 coalesce <code>minChars</code>는 오버라이드되지 않으면 1500으로 상향됩니다.</li></ul><h2 id="블록-사이의-사람스러운-페이싱" tabindex="-1">블록 사이의 사람스러운 페이싱 <a class="header-anchor" href="#블록-사이의-사람스러운-페이싱" aria-label="Permalink to &quot;블록 사이의 사람스러운 페이싱&quot;">​</a></h2><p>블록 스트리밍이 활성화되면, 블록 응답 사이(첫 블록 이후)에 <strong>무작위 지연</strong>을 추가할 수 있습니다. 이는 여러 bubble로 구성된 응답을 보다 자연스럽게 보이게 합니다.</p><ul><li>설정: <code>agents.defaults.humanDelay</code> (에이전트별 오버라이드는 <code>agents.list[].humanDelay</code>를 사용).</li><li>모드: <code>off</code>(기본값), <code>natural</code>(800–2500ms), <code>custom</code>(<code>minMs</code>/<code>maxMs</code>).</li><li><strong>블록 응답</strong>에만 적용됩니다. 최종 응답이나 도구 요약에는 적용되지 않습니다.</li></ul><h2 id="청크를-스트리밍할지-전부-한-번에-스트리밍할지" tabindex="-1">&quot;청크를 스트리밍할지, 전부 한 번에 스트리밍할지&quot; <a class="header-anchor" href="#청크를-스트리밍할지-전부-한-번에-스트리밍할지" aria-label="Permalink to &quot;&quot;청크를 스트리밍할지, 전부 한 번에 스트리밍할지&quot;&quot;">​</a></h2><p>다음으로 매핑됩니다:</p><ul><li><strong>청크 스트리밍:</strong> <code>blockStreamingDefault: &quot;on&quot;</code> + <code>blockStreamingBreak: &quot;text_end&quot;</code> (진행되는 대로 내보냄). Telegram이 아닌 채널은 <code>*.blockStreaming: true</code>도 필요합니다.</li><li><strong>끝에서 전부 스트리밍:</strong> <code>blockStreamingBreak: &quot;message_end&quot;</code> (한 번 플러시; 매우 길면 여러 청크일 수 있음).</li><li><strong>블록 스트리밍 없음:</strong> <code>blockStreamingDefault: &quot;off&quot;</code> (최종 응답만).</li></ul><p><strong>채널 참고:</strong> 블록 스트리밍은 <strong><code>*.blockStreaming</code>이 명시적으로 <code>true</code>로 설정되지 않는 한 꺼져 있습니다.</strong> 채널은 블록 응답 없이 라이브 preview를 스트리밍할 수 있습니다(<code>channels.&lt;channel&gt;.streaming</code>).</p><p>설정 위치 리마인더: <code>blockStreaming*</code> 기본값은 루트 config가 아닌 <code>agents.defaults</code> 아래에 있습니다.</p><h2 id="preview-스트리밍-모드" tabindex="-1">Preview 스트리밍 모드 <a class="header-anchor" href="#preview-스트리밍-모드" aria-label="Permalink to &quot;Preview 스트리밍 모드&quot;">​</a></h2><p>정규 키(canonical key): <code>channels.&lt;channel&gt;.streaming</code></p><p>모드:</p><ul><li><code>off</code>: preview 스트리밍 비활성화.</li><li><code>partial</code>: 단일 preview를 최신 텍스트로 교체.</li><li><code>block</code>: preview를 청크/추가 단계로 업데이트.</li><li><code>progress</code>: 생성 중에는 progress/status preview, 완료 시 최종 답변.</li></ul><h3 id="채널-매핑" tabindex="-1">채널 매핑 <a class="header-anchor" href="#채널-매핑" aria-label="Permalink to &quot;채널 매핑&quot;">​</a></h3><table tabindex="0"><thead><tr><th>채널</th><th><code>off</code></th><th><code>partial</code></th><th><code>block</code></th><th><code>progress</code></th></tr></thead><tbody><tr><td>Telegram</td><td>✅</td><td>✅</td><td>✅</td><td><code>partial</code>로 매핑</td></tr><tr><td>Discord</td><td>✅</td><td>✅</td><td>✅</td><td><code>partial</code>로 매핑</td></tr><tr><td>Slack</td><td>✅</td><td>✅</td><td>✅</td><td>✅</td></tr><tr><td>Mattermost</td><td>✅</td><td>✅</td><td>✅</td><td>✅</td></tr></tbody></table><p>Slack 전용:</p><ul><li><code>channels.slack.streaming.nativeTransport</code>는 <code>channels.slack.streaming.mode=&quot;partial&quot;</code>일 때 Slack 네이티브 스트리밍 API 호출을 토글합니다(기본값: <code>true</code>).</li><li>Slack 네이티브 스트리밍과 Slack assistant 스레드 상태는 reply 스레드 타깃이 필요합니다. Top-level DM은 해당 스레드 스타일 preview를 보여주지 않습니다.</li></ul><p>레거시 키 마이그레이션:</p><ul><li>Telegram: <code>streamMode</code> + boolean <code>streaming</code>은 <code>streaming</code> enum으로 자동 마이그레이션됩니다.</li><li>Discord: <code>streamMode</code> + boolean <code>streaming</code>은 <code>streaming</code> enum으로 자동 마이그레이션됩니다.</li><li>Slack: <code>streamMode</code>는 <code>streaming.mode</code>로 자동 마이그레이션되고, boolean <code>streaming</code>은 <code>streaming.mode</code>와 <code>streaming.nativeTransport</code>로, 레거시 <code>nativeStreaming</code>은 <code>streaming.nativeTransport</code>로 자동 마이그레이션됩니다.</li></ul><h3 id="런타임-동작" tabindex="-1">런타임 동작 <a class="header-anchor" href="#런타임-동작" aria-label="Permalink to &quot;런타임 동작&quot;">​</a></h3><p>Telegram:</p><ul><li>DM과 그룹/토픽 전반에서 <code>sendMessage</code> + <code>editMessageText</code> preview 업데이트를 사용합니다.</li><li>Telegram 블록 스트리밍이 명시적으로 활성화된 경우에는 preview 스트리밍이 건너뛰어집니다(이중 스트리밍 방지).</li><li><code>/reasoning stream</code>은 reasoning을 preview에 쓸 수 있습니다.</li></ul><p>Discord:</p><ul><li>send + edit preview 메시지를 사용합니다.</li><li><code>block</code> 모드는 draft chunking(<code>draftChunk</code>)을 사용합니다.</li><li>Discord 블록 스트리밍이 명시적으로 활성화된 경우에는 preview 스트리밍이 건너뛰어집니다.</li><li>최종 media, error, explicit-reply 페이로드는 새 draft를 플러시하지 않고 대기 중인 preview를 취소한 뒤 일반 전달을 사용합니다.</li></ul><p>Slack:</p><ul><li><code>partial</code>은 가능한 경우 Slack 네이티브 스트리밍(<code>chat.startStream</code>/<code>append</code>/<code>stop</code>)을 사용할 수 있습니다.</li><li><code>block</code>은 append 스타일 draft preview를 사용합니다.</li><li><code>progress</code>는 status preview 텍스트를 사용한 뒤 최종 답변을 사용합니다.</li><li>최종 media/error 페이로드와 progress final은 throwaway draft 메시지를 만들지 않습니다. preview를 편집할 수 있는 텍스트/블록 final만 대기 중인 draft 텍스트를 플러시합니다.</li></ul><p>Mattermost:</p><ul><li>thinking, 도구 활동, 부분 응답 텍스트를 하나의 draft preview 포스트로 스트리밍하며, 최종 답변을 전송해도 안전할 때 그 자리에서 확정(finalize)합니다.</li><li>preview 포스트가 삭제되었거나 finalize 시점에 사용할 수 없는 경우 새 최종 포스트를 전송하도록 폴백합니다.</li><li>최종 media/error 페이로드는 임시 preview 포스트를 플러시하지 않고 대기 중인 preview 업데이트를 취소한 뒤 일반 전달을 사용합니다.</li></ul><p>Matrix:</p><ul><li>Draft preview는 최종 텍스트가 preview 이벤트를 재사용할 수 있을 때 그 자리에서 확정됩니다.</li><li>Media-only, error, reply-target-mismatch final은 일반 전달 전에 대기 중인 preview 업데이트를 취소합니다. 이미 보여진 stale preview는 redact됩니다.</li></ul><h3 id="도구-진행-tool-progress-preview-업데이트" tabindex="-1">도구 진행(tool-progress) preview 업데이트 <a class="header-anchor" href="#도구-진행-tool-progress-preview-업데이트" aria-label="Permalink to &quot;도구 진행(tool-progress) preview 업데이트&quot;">​</a></h3><p>Preview 스트리밍은 <strong>도구 진행(tool-progress)</strong> 업데이트도 포함할 수 있습니다 — &quot;searching the web&quot;, &quot;reading file&quot;, &quot;calling tool&quot; 같은 짧은 상태 줄이 도구 실행 중에 같은 preview 메시지 내에서 최종 응답 이전에 나타납니다. 이는 여러 단계로 이루어진 도구 턴을 시각적으로 살아 있게 유지하여, 첫 thinking preview와 최종 답변 사이에 침묵하지 않도록 합니다.</p><p>지원되는 surface:</p><ul><li><strong>Discord</strong>, <strong>Slack</strong>, <strong>Telegram</strong>은 라이브 preview 편집으로 tool-progress를 스트리밍합니다.</li><li><strong>Mattermost</strong>는 이미 도구 활동을 단일 draft preview 포스트에 통합하고 있습니다(위 참조).</li><li>Tool-progress 편집은 활성 preview 스트리밍 모드를 따릅니다. preview 스트리밍이 <code>off</code>이거나 블록 스트리밍이 메시지를 차지한 경우에는 건너뛰어집니다.</li></ul><h2 id="관련-문서" tabindex="-1">관련 문서 <a class="header-anchor" href="#관련-문서" aria-label="Permalink to &quot;관련 문서&quot;">​</a></h2><ul><li><a href="/openclaw-docs-ko/concepts/messages">메시지</a> — 메시지 라이프사이클 및 전달</li><li><a href="/openclaw-docs-ko/concepts/retry">재시도</a> — 전달 실패 시 재시도 동작</li><li><a href="/openclaw-docs-ko/channels/">채널</a> — 채널별 스트리밍 지원</li></ul>`,56)])])}const h=o(c,[["render",l]]);export{u as __pageData,h as default};
