import{_ as o,o as i,c as l,ag as a}from"./chunks/framework.CTpQozEL.js";const u=JSON.parse('{"title":"모델 failover","description":"OpenClaw가 auth profile을 rotation하고 모델 간 failover하는 방식","frontmatter":{"title":"모델 failover","description":"OpenClaw가 auth profile을 rotation하고 모델 간 failover하는 방식"},"headers":[],"relativePath":"concepts/model-failover.md","filePath":"concepts/model-failover.md","lastUpdated":null}'),r={name:"concepts/model-failover.md"};function t(d,e,s,n,c,p){return i(),l("div",null,[...e[0]||(e[0]=[a(`<p>OpenClaw는 실패를 두 단계로 처리합니다:</p><ol><li>현재 provider 내에서의 <strong>Auth profile rotation</strong>.</li><li><code>agents.defaults.model.fallbacks</code>의 다음 모델로의 <strong>모델 fallback</strong>.</li></ol><p>이 문서는 런타임 규칙과 그것을 뒷받침하는 데이터를 설명합니다.</p><h2 id="런타임-플로우" tabindex="-1">런타임 플로우 <a class="header-anchor" href="#런타임-플로우" aria-label="Permalink to &quot;런타임 플로우&quot;">​</a></h2><p>일반 텍스트 실행의 경우, OpenClaw는 이 순서로 후보(candidate)를 평가합니다:</p><ol><li>현재 선택된 세션 모델.</li><li>구성된 <code>agents.defaults.model.fallbacks</code>를 순서대로.</li><li>실행이 오버라이드에서 시작되었을 때 마지막에 구성된 primary 모델.</li></ol><p>각 후보 내부에서, OpenClaw는 다음 모델 후보로 진행하기 전에 auth-profile failover를 먼저 시도합니다.</p><p>상위 수준 시퀀스:</p><ol><li>활성 세션 모델과 auth-profile 선호도(preference)를 해석(resolve)합니다.</li><li>모델 후보 체인을 빌드합니다.</li><li>auth-profile rotation/cooldown 규칙으로 현재 provider를 시도합니다.</li><li>해당 provider가 failover-worthy 에러로 소진되면, 다음 모델 후보로 이동합니다.</li><li>다른 세션 reader가 runner가 사용하려는 것과 동일한 provider/model을 보도록 재시도가 시작되기 전에 선택된 fallback 오버라이드를 영속화(persist)합니다.</li><li>fallback 후보가 실패하면, 해당 실패한 후보와 여전히 일치할 때에 한해 fallback 소유의 세션 오버라이드 필드만 롤백합니다.</li><li>모든 후보가 실패하면, per-attempt 상세 정보와 알려진 경우 가장 빠른 cooldown 만료 시간을 담은 <code>FallbackSummaryError</code>를 throw합니다.</li></ol><p>이는 &quot;전체 세션을 저장하고 복원&quot;하는 것보다 의도적으로 더 좁은 범위입니다. reply runner는 fallback을 위해 자신이 소유한 모델 선택 필드만 영속화합니다:</p><ul><li><code>providerOverride</code></li><li><code>modelOverride</code></li><li><code>authProfileOverride</code></li><li><code>authProfileOverrideSource</code></li><li><code>authProfileOverrideCompactionCount</code></li></ul><p>이는 실패한 fallback 재시도가 시도가 실행되는 동안 발생한 수동 <code>/model</code> 변경이나 세션 rotation 업데이트와 같은 더 새로운 관련 없는 세션 mutation을 덮어쓰는 것을 방지합니다.</p><h2 id="auth-저장소-키-oauth" tabindex="-1">Auth 저장소 (키 + OAuth) <a class="header-anchor" href="#auth-저장소-키-oauth" aria-label="Permalink to &quot;Auth 저장소 (키 + OAuth)&quot;">​</a></h2><p>OpenClaw는 API 키와 OAuth 토큰 모두에 대해 <strong>auth profile</strong>을 사용합니다.</p><ul><li>Secret은 <code>~/.openclaw/agents/&lt;agentId&gt;/agent/auth-profiles.json</code>에 있습니다 (레거시: <code>~/.openclaw/agent/auth-profiles.json</code>).</li><li>런타임 auth-routing 상태는 <code>~/.openclaw/agents/&lt;agentId&gt;/agent/auth-state.json</code>에 있습니다.</li><li>Config <code>auth.profiles</code> / <code>auth.order</code>는 <strong>metadata + routing만</strong>입니다 (secret 없음).</li><li>레거시 import 전용 OAuth 파일: <code>~/.openclaw/credentials/oauth.json</code> (최초 사용 시 <code>auth-profiles.json</code>으로 import됨).</li></ul><p>자세한 내용: <a href="/openclaw-docs-ko/concepts/oauth">/concepts/oauth</a></p><p>자격 증명(credential) 타입:</p><ul><li><code>type: &quot;api_key&quot;</code> → <code>{ provider, key }</code></li><li><code>type: &quot;oauth&quot;</code> → <code>{ provider, access, refresh, expires, email? }</code> (일부 provider의 경우 + <code>projectId</code>/<code>enterpriseUrl</code>)</li></ul><h2 id="profile-id" tabindex="-1">Profile ID <a class="header-anchor" href="#profile-id" aria-label="Permalink to &quot;Profile ID&quot;">​</a></h2><p>OAuth 로그인은 여러 계정이 공존할 수 있도록 구별되는 profile을 생성합니다.</p><ul><li>기본값: email이 없을 때 <code>provider:default</code>.</li><li>email이 있는 OAuth: <code>provider:&lt;email&gt;</code> (예: <code>google-antigravity:user@gmail.com</code>).</li></ul><p>Profile은 <code>~/.openclaw/agents/&lt;agentId&gt;/agent/auth-profiles.json</code>의 <code>profiles</code> 아래에 있습니다.</p><h2 id="rotation-순서" tabindex="-1">Rotation 순서 <a class="header-anchor" href="#rotation-순서" aria-label="Permalink to &quot;Rotation 순서&quot;">​</a></h2><p>provider에 여러 profile이 있을 때, OpenClaw는 다음과 같은 순서를 선택합니다:</p><ol><li><strong>명시적 config</strong>: <code>auth.order[provider]</code> (설정된 경우).</li><li><strong>구성된 profile</strong>: provider로 필터링된 <code>auth.profiles</code>.</li><li><strong>저장된 profile</strong>: provider에 대한 <code>auth-profiles.json</code>의 항목들.</li></ol><p>명시적 순서가 구성되지 않은 경우, OpenClaw는 round-robin 순서를 사용합니다:</p><ul><li><strong>Primary 키:</strong> profile 타입 (<strong>API 키보다 OAuth 먼저</strong>).</li><li><strong>Secondary 키:</strong> <code>usageStats.lastUsed</code> (각 타입 내에서 가장 오래된 것 먼저).</li><li><strong>Cooldown/비활성화된 profile</strong>은 가장 빠른 만료 순으로 끝으로 이동됩니다.</li></ul><h3 id="세션-stickiness-cache-friendly" tabindex="-1">세션 stickiness (cache-friendly) <a class="header-anchor" href="#세션-stickiness-cache-friendly" aria-label="Permalink to &quot;세션 stickiness (cache-friendly)&quot;">​</a></h3><p>OpenClaw는 provider 캐시를 따뜻하게 유지하기 위해 <strong>선택된 auth profile을 세션당 핀(pin)합니다</strong>. 매 요청마다 rotation하지 <strong>않습니다</strong>. 핀된 profile은 다음까지 재사용됩니다:</p><ul><li>세션이 리셋되는 경우 (<code>/new</code> / <code>/reset</code>)</li><li>compaction이 완료되는 경우 (compaction 카운트 증가)</li><li>profile이 cooldown/비활성화 상태인 경우</li></ul><p><code>/model …@&lt;profileId&gt;</code>를 통한 수동 선택은 해당 세션에 대한 <strong>사용자 오버라이드</strong>를 설정하며 새 세션이 시작될 때까지 자동 rotation되지 않습니다.</p><p>자동으로 핀된 profile (세션 라우터에 의해 선택됨)은 **선호도(preference)**로 취급됩니다: 먼저 시도되지만, OpenClaw는 rate limit/timeout 시 다른 profile로 rotation할 수 있습니다. 사용자 핀된 profile은 해당 profile에 잠긴 채로 유지됩니다; 실패하고 모델 fallback이 구성된 경우, OpenClaw는 profile을 전환하는 대신 다음 모델로 이동합니다.</p><h3 id="oauth가-사라진-것처럼-보이는-이유" tabindex="-1">OAuth가 &quot;사라진 것처럼 보이는&quot; 이유 <a class="header-anchor" href="#oauth가-사라진-것처럼-보이는-이유" aria-label="Permalink to &quot;OAuth가 &quot;사라진 것처럼 보이는&quot; 이유&quot;">​</a></h3><p>동일한 provider에 대해 OAuth profile과 API 키 profile 모두를 가지고 있는 경우, 핀되지 않는 한 round-robin이 메시지 간에 둘 사이를 전환할 수 있습니다. 단일 profile을 강제하려면:</p><ul><li><code>auth.order[provider] = [&quot;provider:profileId&quot;]</code>로 핀하거나,</li><li>profile 오버라이드가 있는 <code>/model …</code>을 통한 per-session 오버라이드를 사용하십시오 (UI/chat surface에서 지원되는 경우).</li></ul><h2 id="cooldown" tabindex="-1">Cooldown <a class="header-anchor" href="#cooldown" aria-label="Permalink to &quot;Cooldown&quot;">​</a></h2><p>profile이 auth/rate-limit 에러 (또는 rate limiting처럼 보이는 timeout)로 실패하면, OpenClaw는 그것을 cooldown으로 표시하고 다음 profile로 이동합니다. 해당 rate-limit 버킷은 단순한 <code>429</code>보다 더 광범위합니다: <code>Too many concurrent requests</code>, <code>ThrottlingException</code>, <code>concurrency limit reached</code>, <code>workers_ai ... quota limit exceeded</code>, <code>throttled</code>, <code>resource exhausted</code>, 그리고 <code>weekly/monthly limit reached</code>와 같은 주기적 usage-window 한도와 같은 provider 메시지도 포함합니다. Format/invalid-request 에러 (예: Cloud Code Assist tool call ID 검증 실패)는 failover-worthy로 취급되며 동일한 cooldown을 사용합니다. <code>Unhandled stop reason: error</code>, <code>stop reason: error</code>, <code>reason: error</code>와 같은 OpenAI-호환 stop-reason 에러는 timeout/failover 신호로 분류됩니다. Provider-scoped 일반 서버 텍스트도 소스가 알려진 transient 패턴과 일치할 때 해당 timeout 버킷에 들어갈 수 있습니다. 예를 들어, Anthropic의 맨(bare) <code>An unknown error occurred</code>와 <code>internal server error</code>, <code>unknown error, 520</code>, <code>upstream error</code>, <code>backend error</code>와 같은 transient 서버 텍스트가 있는 JSON <code>api_error</code> payload는 failover-worthy timeout으로 취급됩니다. OpenRouter 전용의 맨 <code>Provider returned error</code>와 같은 일반적인 upstream 텍스트는 provider 컨텍스트가 실제로 OpenRouter일 때만 timeout으로 취급됩니다. <code>LLM request failed with an unknown error.</code>와 같은 일반 내부 폴백 텍스트는 보수적으로 유지되며 자체적으로 failover를 트리거하지 않습니다.</p><p>일부 provider SDK는 OpenClaw로 제어를 반환하기 전에 긴 <code>Retry-After</code> 창 동안 sleep할 수 있습니다. Anthropic과 OpenAI와 같은 Stainless 기반 SDK의 경우, OpenClaw는 SDK 내부의 <code>retry-after-ms</code> / <code>retry-after</code> 대기 시간을 기본적으로 60초로 제한하고, 이 failover 경로가 실행될 수 있도록 더 긴 retryable 응답을 즉시 surface합니다. <code>OPENCLAW_SDK_RETRY_MAX_WAIT_SECONDS</code>로 제한을 조정하거나 비활성화하십시오; <a href="/openclaw-docs-ko/concepts/retry">/concepts/retry</a>를 참조하십시오.</p><p>Rate-limit cooldown은 model-scoped일 수도 있습니다:</p><ul><li>OpenClaw는 실패한 모델 id가 알려진 경우 rate-limit 실패에 대해 <code>cooldownModel</code>을 기록합니다.</li><li>cooldown이 다른 모델로 범위가 지정된 경우 동일한 provider의 sibling 모델은 여전히 시도될 수 있습니다.</li><li>Billing/비활성화 창은 여전히 모델 전반에 걸쳐 전체 profile을 차단합니다.</li></ul><p>Cooldown은 지수 backoff를 사용합니다:</p><ul><li>1분</li><li>5분</li><li>25분</li><li>1시간 (상한)</li></ul><p>상태는 <code>auth-state.json</code>의 <code>usageStats</code> 아래에 저장됩니다:</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;usageStats&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;provider:profile&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;lastUsed&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1736160000000</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;cooldownUntil&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1736160600000</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;errorCount&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><h2 id="billing-비활성화" tabindex="-1">Billing 비활성화 <a class="header-anchor" href="#billing-비활성화" aria-label="Permalink to &quot;Billing 비활성화&quot;">​</a></h2><p>Billing/credit 실패 (예: &quot;insufficient credits&quot; / &quot;credit balance too low&quot;)는 failover-worthy로 취급되지만, 일반적으로 transient하지 않습니다. 짧은 cooldown 대신, OpenClaw는 profile을 <strong>비활성화</strong>로 표시하고 (더 긴 backoff와 함께) 다음 profile/provider로 rotation합니다.</p><p>모든 billing-shaped 응답이 <code>402</code>는 아니며, 모든 HTTP <code>402</code>가 여기에 해당하는 것도 아닙니다. OpenClaw는 provider가 <code>401</code>이나 <code>403</code>을 대신 반환하더라도 명시적인 billing 텍스트를 billing 레인에 유지하지만, provider-specific matcher는 그것을 소유한 provider로 범위가 지정된 채로 유지됩니다 (예: OpenRouter <code>403 Key limit exceeded</code>). 한편 임시 <code>402</code> usage-window 및 organization/workspace spend-limit 에러는 메시지가 retryable하게 보일 때 <code>rate_limit</code>으로 분류됩니다 (예: <code>weekly usage limit exhausted</code>, <code>daily limit reached, resets tomorrow</code>, <code>organization spending limit exceeded</code>). 이들은 긴 billing-disable 경로 대신 짧은 cooldown/failover 경로에 유지됩니다.</p><p>상태는 <code>auth-state.json</code>에 저장됩니다:</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;usageStats&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;provider:profile&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;disabledUntil&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1736178000000</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;disabledReason&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;billing&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>기본값:</p><ul><li>Billing backoff는 <strong>5시간</strong>에서 시작하여, billing 실패마다 두 배가 되며, <strong>24시간</strong>에서 상한이 걸립니다.</li><li>profile이 <strong>24시간</strong> 동안 실패하지 않으면 backoff 카운터가 리셋됩니다 (구성 가능).</li><li>Overloaded 재시도는 모델 fallback 전에 <strong>1번의 same-provider profile rotation</strong>을 허용합니다.</li><li>Overloaded 재시도는 기본적으로 <strong>0 ms backoff</strong>를 사용합니다.</li></ul><h2 id="모델-fallback" tabindex="-1">모델 fallback <a class="header-anchor" href="#모델-fallback" aria-label="Permalink to &quot;모델 fallback&quot;">​</a></h2><p>provider에 대한 모든 profile이 실패하면, OpenClaw는 <code>agents.defaults.model.fallbacks</code>의 다음 모델로 이동합니다. 이는 auth 실패, rate limit, 그리고 profile rotation이 소진된 timeout에 적용됩니다 (다른 에러는 fallback을 진행시키지 않습니다).</p><p>Overloaded 및 rate-limit 에러는 billing cooldown보다 더 공격적으로 처리됩니다. 기본적으로, OpenClaw는 한 번의 same-provider auth-profile 재시도를 허용한 다음, 기다리지 않고 다음 구성된 모델 fallback으로 전환합니다. <code>ModelNotReadyException</code>과 같은 provider-busy 신호는 해당 overloaded 버킷에 들어갑니다. <code>auth.cooldowns.overloadedProfileRotations</code>, <code>auth.cooldowns.overloadedBackoffMs</code>, <code>auth.cooldowns.rateLimitedProfileRotations</code>로 이를 조정하십시오.</p><p>실행이 모델 오버라이드로 시작되는 경우 (hook 또는 CLI), fallback은 구성된 fallback을 시도한 후 여전히 <code>agents.defaults.model.primary</code>에서 종료됩니다.</p><h3 id="후보-체인-규칙" tabindex="-1">후보 체인 규칙 <a class="header-anchor" href="#후보-체인-규칙" aria-label="Permalink to &quot;후보 체인 규칙&quot;">​</a></h3><p>OpenClaw는 현재 요청된 <code>provider/model</code>과 구성된 fallback으로부터 후보 리스트를 빌드합니다.</p><p>규칙:</p><ul><li>요청된 모델이 항상 첫 번째입니다.</li><li>명시적으로 구성된 fallback은 중복 제거되지만 모델 allowlist로 필터링되지는 않습니다. 명시적 operator 의도로 취급됩니다.</li><li>현재 실행이 이미 동일한 provider 패밀리의 구성된 fallback에 있다면, OpenClaw는 전체 구성된 체인을 계속 사용합니다.</li><li>현재 실행이 config와 다른 provider에 있고 그 현재 모델이 이미 구성된 fallback 체인의 일부가 아니라면, OpenClaw는 다른 provider의 관련 없는 구성된 fallback을 추가하지 않습니다.</li><li>실행이 오버라이드에서 시작된 경우, 이전 후보가 소진된 후 체인이 정상 기본값으로 돌아와 안착할 수 있도록 구성된 primary가 끝에 추가됩니다.</li></ul><h3 id="어떤-에러가-fallback을-진행시키는가" tabindex="-1">어떤 에러가 fallback을 진행시키는가 <a class="header-anchor" href="#어떤-에러가-fallback을-진행시키는가" aria-label="Permalink to &quot;어떤 에러가 fallback을 진행시키는가&quot;">​</a></h3><p>모델 fallback은 다음에서 계속됩니다:</p><ul><li>auth 실패</li><li>rate limit 및 cooldown 소진</li><li>overloaded/provider-busy 에러</li><li>timeout-shaped failover 에러</li><li>billing 비활성화</li><li><code>LiveSessionModelSwitchError</code>, 이는 failover 경로로 정규화되어 stale하게 영속된 모델이 외부 재시도 루프를 만들지 않도록 합니다</li><li>여전히 남은 후보가 있을 때의 다른 인식되지 않은 에러</li></ul><p>모델 fallback은 다음에서는 계속되지 않습니다:</p><ul><li>timeout/failover-shaped가 아닌 명시적 abort</li><li>compaction/retry 로직 내부에 있어야 하는 context overflow 에러 (예: <code>request_too_large</code>, <code>INVALID_ARGUMENT: input exceeds the maximum number of tokens</code>, <code>input token count exceeds the maximum number of input tokens</code>, <code>The input is too long for the model</code>, <code>ollama error: context length exceeded</code>)</li><li>남은 후보가 없을 때의 최종 unknown 에러</li></ul><h3 id="cooldown-skip-대-probe-동작" tabindex="-1">Cooldown skip 대 probe 동작 <a class="header-anchor" href="#cooldown-skip-대-probe-동작" aria-label="Permalink to &quot;Cooldown skip 대 probe 동작&quot;">​</a></h3><p>provider에 대한 모든 auth profile이 이미 cooldown 상태일 때, OpenClaw는 해당 provider를 영원히 자동으로 skip하지는 않습니다. per-candidate 결정을 내립니다:</p><ul><li>지속적인 auth 실패는 전체 provider를 즉시 skip합니다.</li><li>Billing 비활성화는 일반적으로 skip하지만, primary 후보는 여전히 throttle 상에서 probe될 수 있어 재시작 없이 복구가 가능합니다.</li><li>primary 후보는 per-provider throttle과 함께 cooldown 만료 근처에서 probe될 수 있습니다.</li><li>Same-provider fallback sibling은 실패가 transient해 보이는 경우 (<code>rate_limit</code>, <code>overloaded</code>, 또는 unknown) cooldown에도 불구하고 시도될 수 있습니다. 이는 rate limit이 model-scoped이고 sibling 모델이 여전히 즉시 복구될 수 있을 때 특히 관련이 있습니다.</li><li>Transient cooldown probe는 단일 provider가 cross-provider fallback을 지연시키지 않도록 fallback 실행당 provider당 하나로 제한됩니다.</li></ul><h2 id="세션-오버라이드와-live-모델-전환" tabindex="-1">세션 오버라이드와 live 모델 전환 <a class="header-anchor" href="#세션-오버라이드와-live-모델-전환" aria-label="Permalink to &quot;세션 오버라이드와 live 모델 전환&quot;">​</a></h2><p>세션 모델 변경은 공유 상태입니다. 활성 runner, <code>/model</code> 명령, compaction/세션 업데이트, 그리고 live-session reconciliation 모두 동일한 세션 엔트리의 일부를 읽거나 씁니다.</p><p>이는 fallback 재시도가 live 모델 전환과 조율되어야 함을 의미합니다:</p><ul><li>명시적 사용자 주도 모델 변경만 pending live switch를 표시합니다. 여기에는 <code>/model</code>, <code>session_status(model=...)</code>, <code>sessions.patch</code>가 포함됩니다.</li><li>fallback rotation, heartbeat 오버라이드, 또는 compaction과 같은 시스템 주도 모델 변경은 자체적으로 pending live switch를 표시하지 않습니다.</li><li>fallback 재시도가 시작되기 전에, reply runner는 선택된 fallback 오버라이드 필드를 세션 엔트리에 영속화합니다.</li><li>Live-session reconciliation은 stale한 런타임 모델 필드보다 영속된 세션 오버라이드를 선호합니다.</li><li>fallback 시도가 실패하면, runner는 자신이 쓴 오버라이드 필드만 롤백하며, 그 필드가 여전히 실패한 후보와 일치할 때에 한해 그렇습니다.</li></ul><p>이는 고전적인 race condition을 방지합니다:</p><ol><li>Primary가 실패합니다.</li><li>메모리에서 fallback 후보가 선택됩니다.</li><li>세션 저장소는 여전히 이전 primary를 말합니다.</li><li>Live-session reconciliation이 stale한 세션 상태를 읽습니다.</li><li>fallback 시도가 시작되기 전에 재시도가 이전 모델로 되돌아갑니다.</li></ol><p>영속된 fallback 오버라이드가 그 창을 닫으며, 좁은 범위의 롤백은 더 새로운 수동 또는 런타임 세션 변경을 손상시키지 않고 유지합니다.</p><h2 id="관찰-가능성-observability-과-실패-요약" tabindex="-1">관찰 가능성(Observability)과 실패 요약 <a class="header-anchor" href="#관찰-가능성-observability-과-실패-요약" aria-label="Permalink to &quot;관찰 가능성(Observability)과 실패 요약&quot;">​</a></h2><p><code>runWithModelFallback(...)</code>은 로그와 사용자 대상 cooldown 메시징에 공급되는 per-attempt 상세 정보를 기록합니다:</p><ul><li>시도된 provider/model</li><li>이유 (<code>rate_limit</code>, <code>overloaded</code>, <code>billing</code>, <code>auth</code>, <code>model_not_found</code>, 그리고 유사한 failover 이유)</li><li>선택적 status/code</li><li>사람이 읽을 수 있는 에러 요약</li></ul><p>모든 후보가 실패하면, OpenClaw는 <code>FallbackSummaryError</code>를 throw합니다. 외부 reply runner는 그것을 사용하여 &quot;모든 모델이 일시적으로 rate-limit되었습니다&quot;와 같은 더 구체적인 메시지를 빌드하고 알려진 경우 가장 빠른 cooldown 만료 시간을 포함할 수 있습니다.</p><p>해당 cooldown 요약은 model-aware입니다:</p><ul><li>관련 없는 model-scoped rate limit은 시도된 provider/model 체인에 대해 무시됩니다</li><li>남은 차단(block)이 일치하는 model-scoped rate limit이라면, OpenClaw는 그 모델을 여전히 차단하는 마지막 일치 만료 시간을 보고합니다</li></ul><h2 id="관련-구성" tabindex="-1">관련 구성 <a class="header-anchor" href="#관련-구성" aria-label="Permalink to &quot;관련 구성&quot;">​</a></h2><p>다음에 대해서는 <a href="/openclaw-docs-ko/gateway/configuration">Gateway 구성</a>을 참조하십시오:</p><ul><li><code>auth.profiles</code> / <code>auth.order</code></li><li><code>auth.cooldowns.billingBackoffHours</code> / <code>auth.cooldowns.billingBackoffHoursByProvider</code></li><li><code>auth.cooldowns.billingMaxHours</code> / <code>auth.cooldowns.failureWindowHours</code></li><li><code>auth.cooldowns.overloadedProfileRotations</code> / <code>auth.cooldowns.overloadedBackoffMs</code></li><li><code>auth.cooldowns.rateLimitedProfileRotations</code></li><li><code>agents.defaults.model.primary</code> / <code>agents.defaults.model.fallbacks</code></li><li><code>agents.defaults.imageModel</code> 라우팅</li></ul><p>광범위한 모델 선택 및 fallback 개요는 <a href="/openclaw-docs-ko/concepts/models">모델</a>을 참조하십시오.</p>`,84)])])}const k=o(r,[["render",t]]);export{u as __pageData,k as default};
